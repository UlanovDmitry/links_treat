#ifndef TREAT_DIRECTORY_H 
#define TREAT_DIRECTORY_H

#include <string>

class treat_directory
{
private:
    /* data */
public:
    std::string get_message();
    treat_directory(/* args */);
    ~treat_directory();
};





#endif